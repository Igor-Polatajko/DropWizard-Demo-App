server:
  applicationConnectors:
    - type: http
      port: 10000
  adminConnectors:
    - type: http
      port: 10001

swagger:
  resourcePackage: com.ihorpolataiko.dropwizarddemo.resource
  scan: true

mongoProperties:
  dbName: "items_test_db"
  host: "localhost"
  port: 27017
